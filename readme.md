# Fake News Detection using ML

## 📌 Overview
An NLP-based machine learning application that classifies news articles as 'Real' or 'Fake' using the Passive Aggressive Classifier algorithm.

## 🛠️ Tech Stack
- **Language:** Python
- **Libraries:** Pandas, Scikit-Learn, NLTK
- **Frontend:** Streamlit

## 🚀 How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Run the training notebook to generate models.
3. Launch the app: `streamlit run app.py`